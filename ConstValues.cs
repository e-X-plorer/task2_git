namespace Calc
{
    /// <summary>
    /// Class containing fixed values.
    /// </summary>
    public static class ConstValues
    {
        /// <summary>
        /// Values closer to zero than this are considered equal to zero.
        /// </summary>
        public const double ZERO_EPSILON = 0.000001;
    }
}